/**
 * Icônes de l'application.
 *
 * Ré-export nommé depuis lucide-react : un seul endroit à modifier pour changer
 * de jeu d'icônes, et des noms métier plutôt que des noms de pictogrammes.
 * Les emoji restent réservés aux contenus éditoriaux (page d'accueil), jamais
 * à l'interface de travail.
 */
export {
  // Navigation
  LayoutDashboard as IconDashboard,
  Map as IconParcels,
  Sprout as IconCrops,
  Droplets as IconInputs,
  FlaskConical as IconPhyto,
  CloudSun as IconWeather,
  ClipboardList as IconRegistry,
  History as IconHistory,
  FolderOpen as IconDocuments,
  Download as IconExport,
  User as IconProfile,
  Settings as IconSettings,
  LogOut as IconLogout,
  Bell as IconNotification,

  // Actions
  Plus as IconPlus,
  Pencil as IconEdit,
  Trash2 as IconDelete,
  Search as IconSearch,
  Filter as IconFilter,
  X as IconClose,
  Check as IconCheck,
  ChevronRight as IconChevronRight,
  ChevronDown as IconChevronDown,
  ChevronLeft as IconChevronLeft,
  ArrowRight as IconArrowRight,
  ArrowLeft as IconArrowLeft,
  RotateCcw as IconUndo,
  Printer as IconPrint,
  Menu as IconMenu,
  ExternalLink as IconExternal,
  Copy as IconCopy,

  // États et retours
  AlertTriangle as IconWarning,
  AlertCircle as IconError,
  Info as IconInfo,
  CheckCircle2 as IconSuccess,
  Loader2 as IconSpinner,
  ShieldCheck as IconSecurity,

  // Métier
  Ruler as IconArea,
  Tractor as IconOperation,
  Wheat as IconHarvest,
  Calendar as IconCalendar,
  MapPin as IconLocation,
  Layers as IconLayers,
  Building2 as IconFarm,
  FileText as IconFile,
  Image as IconImage,
  Paperclip as IconAttachment,
  Thermometer as IconTemperature,
  Wind as IconWind,
  CloudRain as IconRain,
  Droplet as IconHumidity,
  Gauge as IconPressure,
  TrendingUp as IconTrend,
  Sun as IconSun,
  Moon as IconMoon,
  Monitor as IconSystem,
  Table as IconTable,
  LayoutGrid as IconGrid,
  Satellite as IconSatellite,
  ListChecks as IconTodo,
} from 'lucide-react';

export type { LucideIcon } from 'lucide-react';
